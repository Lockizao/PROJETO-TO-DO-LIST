# Copie este arquivo para .env e preencha com seus próprios valores.
# Nunca commite o .env real — ele contém credenciais.

# String de conexão do MongoDB Atlas
MONGO_URI=mongodb+srv://usuario:senha@seu-cluster.mongodb.net/nome-do-banco

# Porta que o servidor irá rodar
PORT=3000

# Chave secreta usada para assinar os Tokens JWT (gere uma aleatória forte)
JWT_SECRET=troque-por-uma-string-aleatoria-longa
